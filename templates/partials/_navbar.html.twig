<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <!-- Logo -->
        <a class="navbar-brand fw-bold text-primary" href="{{ path('app_home') }}">EasyTrip</a>

        <!-- Mobile Menu Toggle Button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar contents -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="d-flex align-items-center w-100">

                <!-- Centered navigation -->
                <div class="d-flex justify-content-center flex-grow-1">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_blog') }}">Blog</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_about') }}">About us</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="offersDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Our Offers
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="offersDropdown">
                                <li><a class="dropdown-item" href="#">Travels</a></li>
                                <li><a class="dropdown-item" href="#">Hotels</a></li>
                                <li><a class="dropdown-item" href="#">Tickets</a></li>
                                <li><a class="dropdown-item" href="#">Car Rentals</a></li>
                            </ul>
                        </li>
                        <li class="nav-item">
                            {% if app.user and is_granted('ROLE_CLIENT') %}
                                <a class="nav-link" href="{{ path('app_contact') }}">Contact Us</a>
                            {% else %}
                                <a class="nav-link" href="{{ path('app_login') }}">Contact Us</a>
                            {% endif %}
                        </li>
                    </ul>
                </div>

                <!-- Right-side user -->
                <ul class="navbar-nav ms-auto">
                    {% if app.user and (is_granted('ROLE_CLIENT') or is_granted('ROLE_AGENT')) %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                {% if app.user.profilePhoto %}
                                    <img src="{{ app.user.profilePhoto }}" alt="Avatar" class="rounded-circle me-2" style="width: 35px; height: 35px; object-fit: cover;">
                                {% else %}
                                    <img src="https://via.placeholder.com/35" alt="Avatar" class="rounded-circle me-2" style="width: 35px; height: 35px; object-fit: cover;">
                                {% endif %}
                                <div class="d-none d-md-block">
                                    <span class="fw-bold">{{ app.user.name }} {{ app.user.surname }}</span>
                                </div>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                <li><a class="dropdown-item" href="{{ path('profile_update', {'id': app.user.id}) }}">My Account</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Sign In</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-primary ms-2" href="{{ path('app_register') }}">Sign Up</a>
                        </li>
                    {% endif %}
                </ul>

            </div> <!-- End d-flex -->
        </div> <!-- End collapse -->
    </div> <!-- End container -->
</nav>
