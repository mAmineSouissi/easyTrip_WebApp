<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Facture</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 14px; }
        .header { text-align: center; margin-bottom: 40px; }
        table { width: 90%; margin: auto; border-collapse: collapse; margin-bottom: 20px; }
        th, td { padding: 10px; border: 1px solid #ddd; }
        th { background-color: #e3d8c8; color: white; text-transform: uppercase; }
        .footer { text-align: center; font-size: 12px; color: #777; margin-top: 30px; }
    </style>
</head>
<body>

    <div class="header">
        <h1>Facture de votre réservation</h1>
    </div>

    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Téléphone</th>
                <th>Email</th>
                <th>Prix</th>
                <th>Places</th>
            </tr>
        </thead>
        <tbody>
            {% for reservation in reservations %}
                <tr style="background-color: {{ loop.index is odd ? '#f9f9f9' : '#ffffff' }};" onmouseover="this.style.backgroundColor='#f1f1f1';" onmouseout="this.style.backgroundColor='{{ loop.index is odd ? '#f9f9f9' : '#ffffff' }}';">
                    <td>{{ reservation.orderDate|date('Y-m-d') }}</td>
                    <td>{{ reservation.nom }}</td>
                    <td>{{ reservation.prenom }}</td>
                    <td>{{ reservation.phone }}</td>
                    <td>{{ reservation.email }}</td>
                    <td>50.00</td>
                    <td>{{ reservation.places }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" style="text-align: center;">Aucune réservation trouvée.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div id="total-container" style="text-align: right; padding-right: 120px; margin-bottom: 10px; font-weight: bold; font-size: 18px;">
    Total : {{ total|number_format(2, '.', ' ') }} dt
    </div>

    <div class="footer">
        Merci pour votre réservation. À très bientôt !
    </div>

</body>
</html>