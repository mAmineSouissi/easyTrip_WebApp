<form method="post" action="{{ path('app_offer_travel_delete', {'id': offer_travel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer_travel.id) }}">
    <button class="btn">Delete</button>
</form>
