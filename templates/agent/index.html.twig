{% extends 'base.html.twig' %}

{% block title %}Hello AgentController!{% endblock %}

{% block body %}
	<div class="example-wrapper">
		<a href="{{ path('reclamation_index') }}" class="btn btn-primary">
		 Reclamation Management
		</a>
		<a href="{{ path('app_promotion_public') }}" class="btn btn-primary">
			 Promotions List
		</a>
		<a href="{{ path('app_agency_index') }}" class="btn btn-primary">
			 Agency Management
		</a>
		<a href="{{ path('app_offer_travel_index') }}" class="btn btn-primary">
			 Offer Travel Management
		</a>
	</div>
{% endblock %}
